import { DefaultTheme } from "styled-components";
import { BURBPLE_LIGHT, WHITE_GRAY, DARK_GRAY, BLACK, WHITE } from "./color";

export const lightTheme: DefaultTheme = {
  mainBgColor: WHITE,
  textColor: BLACK,
};
export const darkTheme = {
  mainBgColor: DARK_GRAY,
  textColor: WHITE,
};
